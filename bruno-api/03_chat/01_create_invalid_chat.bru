meta {
  name: 01_create_invalid_chat
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/chat/new
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "targetUserId": -1
  }
}

tests {
  test("status 400", function() {
    expect(res.getStatus()).to.equal(400);
  });
}

settings {
  encodeUrl: false
  timeout: 10000
}
