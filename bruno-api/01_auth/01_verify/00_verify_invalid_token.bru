meta {
  name: 00_verify_invalid_token
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/auth/verify?token=invalid
  body: none
  auth: inherit
}

tests {
  test("status 400", function() {
    expect(res.getStatus()).to.equal(400);
  });
}

settings {
  encodeUrl: false
  timeout: 10000
}
