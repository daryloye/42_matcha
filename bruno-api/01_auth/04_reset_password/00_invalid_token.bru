meta {
  name: 00_invalid_token
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/auth/reset-password
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "newPassword": "Password@123",
    "resetToken": "12345"
  }
}

tests {
  test("status 400", function() {
    expect(res.getStatus()).to.equal(400);
  });
}

settings {
  encodeUrl: false
  timeout: 10000
}
