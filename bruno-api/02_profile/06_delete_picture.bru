meta {
  name: 06_delete_picture
  type: http
  seq: 7
}

delete {
  url: {{baseUrl}}/api/profile/picture/{{pictureId}}
  auth: inherit
}

tests {
  test("status 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}

settings {
  encodeUrl: false
  timeout: 10000
}
