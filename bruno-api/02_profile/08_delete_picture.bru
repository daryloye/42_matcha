meta {
  name: 08_delete_picture
  type: http
  seq: 9
}

delete {
  url: {{baseUrl}}/api/profile/pictures/{{pictureId}}
  auth: inherit
}

tests {
  test("status 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}

settings {
  encodeUrl: false
  timeout: 10000
}
