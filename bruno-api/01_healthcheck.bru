meta {
  name: 01_healthcheck
  type: http
  seq: 1
}

get {
  url: http://localhost:5001/health
  body: none
  auth: inherit
}

tests {
  test("status is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
