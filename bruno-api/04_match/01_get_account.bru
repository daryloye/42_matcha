meta {
  name: 01_get_account
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/match/account
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
}

tests {
  test("status 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}

settings {
  encodeUrl: false
  timeout: 10000
}
