meta {
  name: 02_get_connected_users_another_user
  type: http
  seq: 6
}

get {
  url: {{baseUrl}}/api/match/connected
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
}

tests {
  test("status 400", function() {
    expect(res.getStatus()).to.equal(400);
  });
}

settings {
  encodeUrl: false
  timeout: 10000
}
