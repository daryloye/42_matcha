meta {
  name: 03_get_account_data
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/match/connected
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
}

tests {
  test("status 400", function() {
    expect(res.getStatus()).to.equal(400);
  });
}

settings {
  encodeUrl: false
  timeout: 10000
}
