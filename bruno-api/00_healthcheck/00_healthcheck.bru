meta {
  name: 00_healthcheck
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/health
  body: none
  auth: inherit
}

vars:pre-request {
  baseUrl: http://localhost:5001
}

tests {
  test("status is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
